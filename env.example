# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
# Production Database (used by Vercel production deployments)
MONGODB_URI=mongodb+srv://Vercel-Admin-appello-prototype-db:YEkttUnBkYYGtTfq@appello-prototype-db.nrral5a.mongodb.net/appello-tasks-prod?retryWrites=true&w=majority

# Development Database (used for local development)
# Create this database in MongoDB Atlas with the same structure as production
# Use sync-db-schema.js to keep them in sync
MONGODB_DEV_URI=mongodb+srv://Vercel-Admin-appello-prototype-db:YEkttUnBkYYGtTfq@appello-prototype-db.nrral5a.mongodb.net/appello-tasks-dev?retryWrites=true&w=majority

# Test Database (for unit/integration tests)
MONGODB_TEST_URI=mongodb://localhost:27017/appello-tasks-test

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Email Configuration (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@appello.com
SMTP_PASS=your-email-password

# File Upload Configuration
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# Integration with Main Appello Platform
APPELLO_API_BASE_URL=https://api.appello.com
APPELLO_API_KEY=your-appello-api-key

# WebSocket Configuration
SOCKET_IO_ORIGINS=http://localhost:3000,https://app.appello.com

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/app.log
